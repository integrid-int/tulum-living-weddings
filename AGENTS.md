# Agents

## Cursor Cloud specific instructions

This repository (`tulum-living-weddings`) is currently empty (initial commit only, with just a `README.md`). No source code, dependencies, build configuration, or application code exists yet.

### Current state
- **No package manager lockfile** — no `package.json`, `requirements.txt`, or equivalent.
- **No application code** — no services, pages, or components.
- **No tests** — no test framework or test files.
- **No lint/build configuration** — no ESLint, Prettier, TypeScript, or build tool config.

### When code is added
Once the project has source code and a dependency manifest, update the VM startup script (`SetupVmEnvironment`) to install dependencies (e.g., `npm install`, `pnpm install`, `pip install -r requirements.txt`). Add lint, test, and build commands to this file as they become available.
